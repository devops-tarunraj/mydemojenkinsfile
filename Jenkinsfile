pipeline{

    agent any

    stages{

        stage("build"){
 
             steps{
                 echo 'build my jenkinsfile'
             }
        }
        stage("test"){
 
             steps{
                 echo 'build my jenkinsfile'
             }
        }
        stage("deploy"){
 
             steps{
                 echo 'build my jenkinsfile'
             }
        }
